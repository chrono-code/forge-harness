레포 내 `scripts/compaction_probe.sh`에서 Claude Code 세션 전사본(JSONL) 구조를 다루는 부분을 보면, `message.content` 배열 안에서 도구 실행 결과 블록의 `type` 값은 **`"tool_result"`** 입니다 (`b.get('type')=='tool_result'`, `scripts/compaction_probe.sh:90`).
